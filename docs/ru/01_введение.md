# Введение

## Описание библиотеки

**SOTA Recommender Systems Library** - это современная, production-ready Python библиотека для построения рекомендательных систем. Библиотека предоставляет реализации 9 state-of-the-art алгоритмов, от простых но эффективных методов до продвинутых моделей глубокого обучения.

### Ключевые особенности

#### 🎯 Широкий выбор алгоритмов

Библиотека включает три категории моделей:

**Простые но эффективные:**
- **EASE** - Embarrassingly Shallow Autoencoders (супер быстрая модель с закрытым решением)
- **SLIM** - Sparse Linear Methods (интерпретируемая item-item модель)

**Matrix Factorization:**
- **SVD** - Singular Value Decomposition (классическая факторизация)
- **SVD++** - SVD с учетом implicit feedback
- **ALS** - Alternating Least Squares (масштабируемое решение)

**Deep Learning:**
- **NCF** - Neural Collaborative Filtering (GMF + MLP архитектура)
- **LightGCN** - Graph Neural Networks (современный SOTA)
- **SASRec** - Self-Attentive Sequential Recommendations (для последовательных данных)

#### 🚀 Production-ready функции

- **FAISS Integration** - ускорение inference в 10-100 раз
- **REST API** - готовый FastAPI сервис для deployment
- **Inference Optimization** - кэширование, batching, ensemble
- **Comprehensive Evaluation** - 15+ метрик качества
- **Flexible Data Processing** - загрузчики датасетов, препроцессинг, negative sampling

#### 💡 Единый API

Все модели следуют единому интерфейсу:

```python
# Единый API для всех моделей
model = ModelClass(**hyperparameters)
model.fit(train_data)
recommendations = model.recommend(user_ids, k=10)
predictions = model.predict(user_ids, item_ids)
model.save('model.pkl')
```

### Кому подходит эта библиотека?

- **Исследователям** - для экспериментов с SOTA алгоритмами
- **Data Scientists** - для быстрого прототипирования рекомендательных систем
- **ML Engineers** - для production deployment с готовыми оптимизациями
- **Студентам** - для изучения современных методов рекомендаций

## Требования и установка

### Системные требования

**Минимальные:**
- Python 3.8+
- 4 GB RAM
- CPU

**Рекомендуемые для deep learning моделей:**
- Python 3.8+
- 16 GB RAM
- NVIDIA GPU с CUDA 11.0+
- 50+ GB свободного места (для больших датасетов)

### Базовая установка

Для установки базовой версии (EASE, SLIM, SVD, SVD++, ALS):

```bash
# Клонирование репозитория
git clone https://github.com/hichnicksemen/svd-recommender.git
cd svd-recommender

# Установка базовых зависимостей
pip install numpy pandas scipy scikit-learn

# Установка библиотеки
pip install -e .
```

Базовая установка включает:
- NumPy, Pandas для работы с данными
- SciPy для разреженных матриц
- Scikit-learn для метрик и утилит

### Установка с PyTorch (для deep learning моделей)

Для использования NCF, LightGCN, SASRec необходим PyTorch:

```bash
# Установка всех зависимостей включая PyTorch
pip install -r requirements.txt

# Или установка PyTorch отдельно (CPU)
pip install torch torchvision torchaudio

# Для GPU (CUDA 11.8)
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
```

### Опциональные зависимости

#### FAISS (для быстрого поиска)

FAISS ускоряет inference в 10-100 раз для больших каталогов:

```bash
# CPU версия
pip install faiss-cpu

# GPU версия (требуется CUDA)
pip install faiss-gpu
```

#### FastAPI (для model serving)

Для запуска REST API сервиса:

```bash
pip install fastapi uvicorn[standard]
```

#### Optuna (для подбора гиперпараметров)

Для автоматической оптимизации гиперпараметров:

```bash
pip install optuna
```

### Полная установка

Установка всех возможных зависимостей:

```bash
pip install -r requirements.txt
pip install faiss-gpu fastapi uvicorn optuna
```

### Проверка установки

Проверьте, что библиотека установлена корректно:

```python
import recommender

print(f"Версия библиотеки: {recommender.__version__}")

# Проверка доступных моделей
from recommender import (
    EASERecommender,
    SLIMRecommender,
    SVDRecommender,
    ALSRecommender
)

# Проверка PyTorch моделей
try:
    from recommender import NCFRecommender, LightGCNRecommender, SASRecRecommender
    print("✅ PyTorch модели доступны")
except ImportError:
    print("⚠️  PyTorch модели недоступны (установите PyTorch)")

# Проверка FAISS
try:
    import faiss
    print("✅ FAISS доступен")
except ImportError:
    print("⚠️  FAISS недоступен (опционально)")

print("\n✅ Библиотека установлена корректно!")
```

## Быстрый старт

Давайте создадим простую рекомендательную систему за 5 минут!

### Шаг 1: Импорты

```python
from recommender import (
    EASERecommender,
    load_movielens,
    InteractionDataset,
    Evaluator
)
```

### Шаг 2: Загрузка данных

```python
# Загрузка датасета MovieLens 100K
df = load_movielens(size='100k')

print(f"Загружено {len(df)} взаимодействий")
print(f"Пользователей: {df['user_id'].nunique()}")
print(f"Items: {df['item_id'].nunique()}")
```

### Шаг 3: Подготовка данных

```python
# Создание датасета для implicit feedback
dataset = InteractionDataset(
    df,
    implicit=True,  # Преобразовать в implicit (бинарный)
    min_user_interactions=5  # Фильтрация редких пользователей
)

# Разделение на train и test
train_data, test_data = dataset.split(
    test_size=0.2,
    strategy='random',
    seed=42
)

print(f"Train: {len(train_data)} взаимодействий")
print(f"Test: {len(test_data)} взаимодействий")
```

### Шаг 4: Обучение модели

```python
# Создание и обучение EASE модели
model = EASERecommender(l2_reg=500.0)

print("Обучение модели...")
model.fit(train_data.data)
print("✅ Модель обучена!")
```

### Шаг 5: Генерация рекомендаций

```python
# Рекомендации для первых 5 пользователей
user_ids = [1, 2, 3, 4, 5]
recommendations = model.recommend(
    user_ids,
    k=10,
    exclude_seen=True  # Исключить уже просмотренные items
)

# Вывод рекомендаций
for user_id, items in recommendations.items():
    item_ids = [item_id for item_id, score in items[:5]]
    print(f"User {user_id}: {item_ids}")
```

### Шаг 6: Оценка качества

```python
# Создание evaluator
evaluator = Evaluator(
    metrics=['precision', 'recall', 'ndcg'],
    k_values=[5, 10, 20]
)

# Оценка модели
results = evaluator.evaluate(
    model,
    test_data=test_data,
    task='ranking',
    exclude_train=True,
    train_data=train_data
)

# Вывод результатов
evaluator.print_results(results)
```

### Шаг 7: Сохранение модели

```python
# Сохранение обученной модели
model.save('ease_model.pkl')

# Загрузка модели
loaded_model = EASERecommender()
loaded_model.load('ease_model.pkl')
```

### Полный пример

Вот полный код для копирования:

```python
from recommender import (
    EASERecommender,
    load_movielens,
    InteractionDataset,
    Evaluator
)

# Загрузка данных
df = load_movielens(size='100k')

# Подготовка
dataset = InteractionDataset(df, implicit=True, min_user_interactions=5)
train_data, test_data = dataset.split(test_size=0.2, strategy='random', seed=42)

# Обучение
model = EASERecommender(l2_reg=500.0)
model.fit(train_data.data)

# Рекомендации
recommendations = model.recommend([1, 2, 3], k=10, exclude_seen=True)
for user_id, items in recommendations.items():
    print(f"User {user_id}: {[item_id for item_id, _ in items[:5]]}")

# Оценка
evaluator = Evaluator(metrics=['precision', 'recall', 'ndcg'], k_values=[10])
results = evaluator.evaluate(model, test_data, task='ranking', train_data=train_data)
evaluator.print_results(results)

# Сохранение
model.save('ease_model.pkl')
```

## Архитектура библиотеки

### Структура модулей

Библиотека организована в модульную архитектуру:

```
recommender/
├── core/                    # Базовые компоненты
│   ├── base.py             # BaseRecommender, ImplicitRecommender, ExplicitRecommender
│   ├── data.py             # InteractionDataset
│   └── trainers.py         # PyTorchTrainer (для deep learning)
│
├── models/                  # Все модели
│   ├── simple/             # EASE, SLIM
│   ├── factorization/      # SVD, SVD++, ALS
│   ├── neural/             # NCF
│   ├── graph/              # LightGCN
│   └── sequential/         # SASRec
│
├── data/                    # Работа с данными
│   ├── datasets.py         # Загрузчики датасетов
│   ├── preprocessing.py    # Препроцессинг
│   └── samplers.py         # Negative sampling
│
├── evaluation/             # Оценка моделей
│   ├── metrics.py          # Метрики качества
│   └── evaluator.py        # Evaluator класс
│
├── utils/                  # Production утилиты
│   ├── inference.py        # Оптимизация inference
│   └── faiss_index.py      # FAISS интеграция
│
└── serving/                # Model serving
    └── api.py              # FastAPI сервис
```

### Базовые классы

#### BaseRecommender

Абстрактный базовый класс для всех моделей:

```python
class BaseRecommender:
    def fit(self, interactions):
        """Обучение модели на данных взаимодействий"""
        raise NotImplementedError
    
    def predict(self, user_ids, item_ids):
        """Предсказание scores для пар user-item"""
        raise NotImplementedError
    
    def recommend(self, user_ids, k=10, exclude_seen=False):
        """Генерация top-K рекомендаций"""
        raise NotImplementedError
    
    def save(self, path):
        """Сохранение модели"""
        pass
    
    def load(self, path):
        """Загрузка модели"""
        pass
```

#### ImplicitRecommender

Базовый класс для моделей implicit feedback (EASE, SLIM, ALS):

```python
class ImplicitRecommender(BaseRecommender):
    """
    Модели для implicit feedback (бинарных взаимодействий)
    Примеры: клики, просмотры, покупки
    """
```

#### ExplicitRecommender

Базовый класс для моделей explicit feedback (SVD, SVD++):

```python
class ExplicitRecommender(BaseRecommender):
    """
    Модели для explicit feedback (рейтингов)
    Примеры: оценки 1-5 звёзд
    """
```

### InteractionDataset

Класс для работы с данными взаимодействий:

```python
class InteractionDataset:
    def __init__(self, df, implicit=True, min_user_interactions=0):
        """
        Args:
            df: DataFrame с колонками user_id, item_id, [rating], [timestamp]
            implicit: Бинаризовать данные для implicit feedback
            min_user_interactions: Минимум взаимодействий на пользователя
        """
    
    def split(self, test_size=0.2, val_size=0.0, strategy='random'):
        """Разделение на train/val/test"""
    
    def to_csr_matrix(self):
        """Конвертация в разреженную CSR матрицу"""
    
    def get_user_items(self, user_id):
        """Получить items для пользователя"""
```

### Pipeline обработки

Типичный pipeline работы с библиотекой:

```
1. Загрузка данных
   ↓
2. Создание InteractionDataset
   ↓
3. Препроцессинг (фильтрация, нормализация)
   ↓
4. Разделение на train/test
   ↓
5. Выбор модели
   ↓
6. Обучение модели
   ↓
7. Генерация рекомендаций
   ↓
8. Оценка качества
   ↓
9. Production deployment (FAISS, API)
```

### Расширяемость

Библиотека легко расширяется. Для добавления своей модели:

```python
from recommender.core import BaseRecommender

class MyRecommender(BaseRecommender):
    def __init__(self, my_param=1.0):
        super().__init__()
        self.my_param = my_param
    
    def fit(self, interactions):
        # Ваша логика обучения
        pass
    
    def predict(self, user_ids, item_ids):
        # Ваша логика предсказания
        pass
    
    def recommend(self, user_ids, k=10, exclude_seen=False):
        # Ваша логика рекомендаций
        pass
```

## Что дальше?

После прочтения этого введения, рекомендуем:

1. **[Подготовка данных](02_подготовка_данных.md)** - узнайте про загрузку и препроцессинг данных
2. **[Простые модели](03_модели_простые.md)** - начните с EASE и SLIM
3. **[Примеры](10_примеры.md)** - посмотрите полные end-to-end примеры

### Дополнительные ресурсы

- [GitHub Repository](https://github.com/hichnicksemen/svd-recommender)
- [Examples Directory](../../examples/)
- [Tests](../../tests/)
- [English Documentation](../../README.md)

---

**Следующая глава**: [Подготовка данных →](02_подготовка_данных.md)

